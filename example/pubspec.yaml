name: example
description: FAB Circular Menu Example

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  fab_circular_menu:
    path: ./../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true